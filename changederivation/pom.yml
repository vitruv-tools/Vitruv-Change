modelEncoding: UTF-8
modelVersion: 4.0.0

parent:
  relativePath: ../pom.yml
  groupId: tools.vitruv
  artifactId: tools.vitruv.change
  version: 3.2.0-SNAPSHOT

artifactId: tools.vitruv.change.changederivation
name: Vitruv Change Derivation
description: ''

build:
  plugins:
  - groupId: org.codehaus.mojo
    artifactId: build-helper-maven-plugin

  - groupId: org.eclipse.xtend
    artifactId: xtend-maven-plugin

dependencies:
# project dependencies
- { groupId: '${project.groupId}', artifactId: tools.vitruv.change.atomic, version: '${project.version}' }
- { groupId: '${project.groupId}', artifactId: tools.vitruv.change.composite, version: '${project.version}' }

# external dependencies
- { groupId: com.google.guava, artifactId: guava }
- { groupId: org.eclipse.emf, artifactId: org.eclipse.emf.common }
- { groupId: org.eclipse.emf, artifactId: org.eclipse.emf.ecore }
- { groupId: org.eclipse.xtext, artifactId: org.eclipse.xtext.xbase.lib }

# p2 dependencies
- { groupId: '${project.groupId}', artifactId: tools.vitruv.change.p2wrappers.emfcompare, version: '${project.version}' }
- { groupId: '${project.groupId}', artifactId: tools.vitruv.change.p2wrappers.emfutils, version: '${project.version}' }
